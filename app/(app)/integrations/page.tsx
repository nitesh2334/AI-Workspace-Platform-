import { PlaceholderPanel } from "@/components/cortex/placeholder-panel";

export default function IntegrationsPage() {
  return (
    <PlaceholderPanel
      eyebrow="Integrations"
      title="Bring vendors, data planes, and identity together."
      description="No APIs yet—just a polished placeholder so the sidebar never dead-ends."
    />
  );
}

