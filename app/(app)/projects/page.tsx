import { PlaceholderPanel } from "@/components/cortex/placeholder-panel";

export default function ProjectsPage() {
  return (
    <PlaceholderPanel
      eyebrow="Projects"
      title="Coordinate prompts, datasets, and releases."
      description="This route exists so navigation stays pleasant—swap in your real project surfaces when you are ready."
    />
  );
}

