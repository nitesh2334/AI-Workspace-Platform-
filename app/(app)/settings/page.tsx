import { PlaceholderPanel } from "@/components/cortex/placeholder-panel";

export default function SettingsPage() {
  return (
    <PlaceholderPanel
      eyebrow="Workspace settings"
      title="Policies, budgets, and collaboration defaults."
      description="Future controls live here—today it mirrors the shell aesthetic without pretending to persist state."
    />
  );
}

