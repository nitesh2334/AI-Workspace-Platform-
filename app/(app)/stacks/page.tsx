import { PlaceholderPanel } from "@/components/cortex/placeholder-panel";

export default function StacksPage() {
  return (
    <PlaceholderPanel
      eyebrow="Stacks"
      title="Compose models, tools, and environments."
      description="Stacks are the backbone of Cortex—this placeholder keeps the shell coherent until you wire composition."
    />
  );
}

