import { PlaceholderPanel } from "@/components/cortex/placeholder-panel";

export default function WorkflowsPage() {
  return (
    <PlaceholderPanel
      eyebrow="Workflows"
      title="Deterministic paths with crisp telemetry."
      description="Automation surfaces land here—today it is a calm placeholder with product-grade spacing."
    />
  );
}

